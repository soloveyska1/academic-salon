---
import Base from '../layouts/Base.astro';
import { breadcrumb } from '../lib/schema';
import {
  ANTI_AI_TIERS,
  ANTI_AI_URGENCIES,
  getAntiAiPrice,
  getAntiAiEntryHint,
} from '../lib/order-price-hint.js';

const entryHint = getAntiAiEntryHint();
const entryNumber = entryHint.replace('от ', '').replace(' ₽', '').trim();

const tierEntries = Object.entries(ANTI_AI_TIERS) as [string, typeof ANTI_AI_TIERS[keyof typeof ANTI_AI_TIERS]][];
const urgencyEntries = Object.entries(ANTI_AI_URGENCIES) as [string, typeof ANTI_AI_URGENCIES[keyof typeof ANTI_AI_URGENCIES]][];

const faqItems = [
  {
    q: 'Можно ли переписать работу самостоятельно?',
    a: 'Технически — да. На практике это занимает несколько дней с многократными прогонами через детектор: вносится правка, дожидается результат, корректируется снова. Мы выполняем тот же объём за один рабочий день и фиксируем результат отчётом «до / после».',
  },
  {
    q: 'Снизится ли уникальность по основному антиплагиату?',
    a: 'Нет. Изменяются только стилистические маркеры — длинное тире, типовые обороты, ритм синтаксиса. По смыслу и фактическому совпадению с источниками текст остаётся прежним; уникальность по «Антиплагиат.ВУЗ» сохраняется на том же уровне, что была до правки.',
  },
  {
    q: 'Что входит в отчёт «до / после»?',
    a: 'PDF-документ на 2–3 страницы: маркированные фрагменты исходного текста, рядом — выполненные правки и краткое обоснование. Документ можно предъявить научному руководителю при обсуждении работы; он показывает характер изменений и подтверждает сохранение смысла.',
  },
  {
    q: 'Возможно ли согласовать стилистические ограничения кафедры?',
    a: 'Да. На юридических, философских и некоторых других направлениях формальные академические обороты являются частью требований; в этих случаях штампы остаются там, где они содержательно необходимы. Ограничения указываются в заявке либо в комментарии — будут учтены при редактуре.',
  },
  {
    q: 'Какие предусмотрены гарантии и условия возврата?',
    a: 'Если после правки «Антиплагиат 2.0» показывает долю ИИ выше 30%, мы дорабатываем работу без дополнительной оплаты до достижения целевого порога. Если кафедра требует строго формальный стиль, в котором маркеры детектора объективно неустранимы, стоимость работы возвращается полностью.',
  },
];

const faqLd = {
  '@context': 'https://schema.org',
  '@type': 'FAQPage',
  mainEntity: faqItems.map((item) => ({
    '@type': 'Question',
    name: item.q,
    acceptedAnswer: { '@type': 'Answer', text: item.a },
  })),
};

const serviceLd = {
  '@context': 'https://schema.org',
  '@type': 'Service',
  serviceType: 'Очистка академической работы от ИИ-маркеров',
  name: 'Чистка под Антиплагиат 2.0 — Академический Салон',
  description: 'Чистка академической работы под детектор «Антиплагиат 2.0»: убираем стилистические маркеры — длинное тире, типовые обороты, шаблонный синтаксис. Срок — один рабочий день, отчёт «до / после» включён в стоимость. От 2 500 ₽.',
  provider: {
    '@type': 'Organization',
    name: 'Академический Салон',
    url: 'https://bibliosaloon.ru/',
  },
  areaServed: { '@type': 'Country', name: 'Россия' },
  availableChannel: {
    '@type': 'ServiceChannel',
    serviceUrl: 'https://bibliosaloon.ru/order/?service=anti-ai',
    serviceName: 'Калькулятор и заявка',
  },
  offers: {
    '@type': 'Offer',
    priceCurrency: 'RUB',
    price: '2500',
    description: 'Базовая ставка для эссе и рефератов до 15 страниц. Большие работы и срочность — по тарифной матрице.',
  },
};
---

<Base
  title="Чистка под Антиплагиат 2.0 — уберём следы ChatGPT | Академический Салон"
  description="«Антиплагиат 2.0» оценивает стилистические привычки текста — длинное тире, типовые обороты, ритм фраз. Маркеры остаются заметными даже в самостоятельных работах. Чистим за один день, смысл и термины сохраняем. От 2 500 ₽, отчёт «до / после» включён."
  currentPath="/anti-ai"
  extraSchema={[
    breadcrumb([{ name: 'Чистка от ИИ', url: '/anti-ai/' }]),
    serviceLd,
    faqLd,
  ]}
>

  <article class="aai">

    <!-- ═══════ HERO ═══════ -->
    <section class="aai-hero" aria-labelledby="aai-hero-h">

      <div class="aai-eyebrow" aria-hidden="false">
        <span class="rule"></span>
        <span class="text">Антиплагиат 2.0 · апрель 2026 · пилот в 50 вузах</span>
      </div>

      <header class="aai-head">
        <div class="col">
          <span class="lbl">Срок</span>
          <span class="val">1 <i class="it">день</i></span>
        </div>
        <div class="col">
          <span class="lbl">Цена</span>
          <span class="val">{entryNumber} <i class="it">₽+</i></span>
        </div>
        <div class="col">
          <span class="lbl">Бонус</span>
          <span class="val">отчёт <i class="it">до / после</i></span>
        </div>
      </header>

      <h1 id="aai-hero-h" class="aai-h">
        <span class="ln">Уберём <em class="it">следы ChatGPT</em> —</span>
        <span class="ln-2">текст пройдёт <em class="it">«Антиплагиат 2.0»</em></span>
      </h1>

      <p class="aai-lede">
        С апреля 2026 «Антиплагиат» оценивает не только совпадения, но и
        стилистические привычки текста: ритм фраз, академические штампы,
        пунктуацию. Маркеры остаются заметными даже в самостоятельных работах.
        Чистим за день — смысл, термины и структуру сохраняем без изменений.
      </p>

      <div class="aai-actions">
        <a href="/order?service=anti-ai" class="btn btn-gold btn-lg">
          Рассчитать — {entryHint}
        </a>
        <a href="https://t.me/academicsaloon" target="_blank" rel="noopener" class="btn btn-ghost-gold btn-lg">
          Обсудить в Telegram
        </a>
      </div>

      <a href="#aai-pain" class="aai-scroll-cue" aria-label="Читать дальше">
        <span class="lbl">как работает детектор</span>
        <span class="line" aria-hidden="true"></span>
      </a>

    </section>

    <!-- ═══════ I — НАБЛЮДЕНИЯ ═══════ -->
    <section class="aai-section aai-pain" id="aai-pain" aria-labelledby="aai-pain-h">
      <header class="aai-chapter-head">
        <span class="aai-chapter-num" aria-hidden="true">I</span>
        <div class="aai-chapter-body">
          <span class="aai-chapter-eyebrow">Что мы видим в апреле 2026</span>
          <h2 id="aai-pain-h" class="aai-chapter-h">
            Три типа обращений <em class="it">с одной природой</em>.
          </h2>
        </div>
      </header>

      <div class="aai-pain-quotes">
        <figure class="aai-quote">
          <blockquote>Работа проходит детектор с долей ИИ около 60%. После переписывания показатель снижается до 45%, очередная правка возвращает его в красную зону.</blockquote>
          <figcaption>Цикл переписывания · ВКР, психология</figcaption>
        </figure>
        <figure class="aai-quote">
          <blockquote>До защиты остаётся две недели. Студент третий месяц вносит правки в один и тот же диплом без устойчивого результата по детектору.</blockquote>
          <figcaption>Сжатые сроки · магистратура</figcaption>
        </figure>
        <figure class="aai-quote">
          <blockquote>Текст написан полностью самостоятельно, без использования генеративных моделей. «Антиплагиат 2.0» помечает работу как сгенерированную.</blockquote>
          <figcaption>Самостоятельная работа · социальная работа</figcaption>
        </figure>
      </div>

      <p class="aai-pain-note">
        С запуска пилота <em class="it">«Антиплагиат 2.0»</em> такие случаи стали ежедневной нормой. Причина — общая, и она поддаётся точечной правке.
      </p>
    </section>

    <!-- ═══════ II — ЛОГИКА ДЕТЕКТОРА ═══════ -->
    <section class="aai-section aai-why" id="aai-why" aria-labelledby="aai-why-h">
      <header class="aai-chapter-head">
        <span class="aai-chapter-num" aria-hidden="true">II</span>
        <div class="aai-chapter-body">
          <span class="aai-chapter-eyebrow">Логика «Антиплагиата 2.0» · что фиксирует детектор</span>
          <h2 id="aai-why-h" class="aai-chapter-h">
            Оценивается не смысл, <em class="it">а стилистическая поверхность</em>.
          </h2>
        </div>
      </header>

      <div class="aai-why-cols">
        <article class="aai-why-col">
          <span class="aai-why-mark" aria-hidden="true">I</span>
          <h3>Ритм фраз</h3>
          <p>
            Большие языковые модели тяготеют к средне-длинному предложению с
            двумя зависимыми частями. Совокупный синтаксический ритм становится
            распознаваемым паттерном — независимо от темы и фактического
            содержания. По данным «Антиплагиата», доля работ с признаками ИИ
            за год выросла с&nbsp;16%&nbsp;до&nbsp;23%.
          </p>
          <p class="aai-why-src">
            <a href="https://antiplagiat.ru/news/antiplagiat2/" target="_blank" rel="noopener">Антиплагиат →</a>
            <a href="https://www.interfax.ru/russia/1052546" target="_blank" rel="noopener">Интерфакс →</a>
          </p>
        </article>

        <article class="aai-why-col">
          <span class="aai-why-mark" aria-hidden="true">II</span>
          <h3>Академические штампы</h3>
          <p>
            «Важно отметить», «в современном мире», «не просто X — а Y» и
            подобные обороты входят в курс академического письма, поэтому
            закономерно встречаются в самостоятельных текстах. Детектор
            оценивает их концентрацию по разделам. В системе выделено
            двенадцать групп таких маркеров.
          </p>
          <p class="aai-why-src">
            <a href="https://habr.com/ru/articles/1022906/" target="_blank" rel="noopener">Хабр · разбор маркеров →</a>
          </p>
        </article>

        <article class="aai-why-col">
          <span class="aai-why-mark" aria-hidden="true">III</span>
          <h3>Пунктуация и форма</h3>
          <p>
            Длинное тире, кавычки-«ёлочки», последовательное использование
            «ё». В ноябре 2025 OpenAI добавили в ChatGPT отдельную опцию
            отключения длинного тире — публично подтверждая, что эта
            пунктуация служит маркером модели и попадает в выдачу детекторов.
          </p>
          <p class="aai-why-src">
            <a href="https://techcrunch.com/2025/11/14/openai-says-its-fixed-chatgpts-em-dash-problem/" target="_blank" rel="noopener">TechCrunch · OpenAI →</a>
            <a href="https://www.cnews.ru/news/line/2025-10-14_v_rossii_poyavilsya_pervyj" target="_blank" rel="noopener">CNews · Word + Google Docs →</a>
          </p>
        </article>
      </div>
    </section>

    <!-- ═══════ III — ПРОЦЕСС ═══════ -->
    <section class="aai-section aai-process" id="aai-process" aria-labelledby="aai-process-h">
      <header class="aai-chapter-head">
        <span class="aai-chapter-num" aria-hidden="true">III</span>
        <div class="aai-chapter-body">
          <span class="aai-chapter-eyebrow">Процесс · от заявки до отчёта</span>
          <h2 id="aai-process-h" class="aai-chapter-h">
            Четыре этапа · <em class="it">смысл сохраняем</em>.
          </h2>
        </div>
      </header>

      <ol class="aai-steps">
        <li class="aai-step">
          <span class="aai-step-n" aria-hidden="true">I</span>
          <div class="aai-step-body">
            <h3>Заявка и материалы</h3>
            <p>Принимаем DOC, DOCX и PDF — до шести файлов общим объёмом до 45 МБ. К работе прикладываются методические требования кафедры и любые стилистические ограничения, которые необходимо учесть при редактуре.</p>
          </div>
        </li>
        <li class="aai-step">
          <span class="aai-step-n" aria-hidden="true">II</span>
          <div class="aai-step-body">
            <h3>Анализ маркеров</h3>
            <p>Текст пропускается через симулятор детектора. Определяются разделы и абзацы с высокой концентрацией маркеров «Антиплагиата 2.0»: длинное тире, типовые обороты, шаблонный синтаксический ритм. Результат — карта риска по структуре работы.</p>
          </div>
        </li>
        <li class="aai-step">
          <span class="aai-step-n" aria-hidden="true">III</span>
          <div class="aai-step-body">
            <h3>Редактура</h3>
            <p>Стилистические маркеры заменяются на нейтральные эквиваленты, ритм фраз варьируется, пунктуация приводится к академически нейтральной норме. Терминология, структура и фактическое содержание не затрагиваются.</p>
          </div>
        </li>
        <li class="aai-step">
          <span class="aai-step-n" aria-hidden="true">IV</span>
          <div class="aai-step-body">
            <h3>Передача и отчёт</h3>
            <p>Готовый файл сопровождается PDF-отчётом «до / после»: подсветка изменений, краткое обоснование каждой правки. Документ можно использовать при обсуждении работы с научным руководителем.</p>
          </div>
        </li>
      </ol>
    </section>

    <!-- ═══════ IV — ЦЕНА ═══════ -->
    <section class="aai-section aai-pricing" id="aai-pricing" aria-labelledby="aai-pricing-h">
      <header class="aai-chapter-head">
        <span class="aai-chapter-num" aria-hidden="true">IV</span>
        <div class="aai-chapter-body">
          <span class="aai-chapter-eyebrow">Сколько стоит · ясно и без скидок-обманок</span>
          <h2 id="aai-pricing-h" class="aai-chapter-h">
            Цена — <em class="it">прозрачная матрица</em>.
          </h2>
        </div>
      </header>

      <div class="aai-pricing-table-wrap">
        <table class="aai-pricing-table" aria-label="Прайс «Чистка под Антиплагиат 2.0»">
          <thead>
            <tr>
              <th class="aai-pricing-corner" aria-hidden="true"></th>
              {urgencyEntries.map(([key, u]) => (
                <th scope="col">
                  <span class="th-l">{u.label}</span>
                  <span class="th-s">{u.desc}{u.multiplier !== 1 ? ` · ×${u.multiplier}` : ''}</span>
                </th>
              ))}
            </tr>
          </thead>
          <tbody>
            {tierEntries.map(([tierKey, t]) => (
              <tr>
                <th scope="row" class="aai-pricing-row-h">
                  <span class="rh-l">{t.label}</span>
                  <span class="rh-s">{t.pages}</span>
                </th>
                {urgencyEntries.map(([urgKey, u]) => {
                  const price = getAntiAiPrice(tierKey, urgKey);
                  if (price == null) {
                    return (
                      <td class="aai-pricing-na" aria-label="Недоступно">—</td>
                    );
                  }
                  return (
                    <td>
                      <a
                        class="aai-pricing-cell"
                        href={`/order?service=anti-ai&tier=${tierKey}&urgency=${urgKey}`}
                      >
                        {price.toLocaleString('ru-RU')} ₽
                      </a>
                    </td>
                  );
                })}
              </tr>
            ))}
          </tbody>
        </table>
      </div>

      <p class="aai-pricing-note">
        Цены финальные. Отчёт «до / после» включён в стоимость. Оплата — после
        согласования объёма работы и срока. Срочные тарифы (×2 и ×3) подразумевают
        параллельную работу нескольких редакторов и согласовываются <em class="it">с учётом текущей загрузки мастерской</em>.
      </p>
    </section>

    <!-- ═══════ V — FAQ ═══════ -->
    <section class="aai-section aai-faq" id="aai-faq" aria-labelledby="aai-faq-h">
      <header class="aai-chapter-head">
        <span class="aai-chapter-num" aria-hidden="true">V</span>
        <div class="aai-chapter-body">
          <span class="aai-chapter-eyebrow">Перед заказом · уточнения</span>
          <h2 id="aai-faq-h" class="aai-chapter-h">
            Пять вопросов, <em class="it">которые задают чаще всего</em>.
          </h2>
        </div>
      </header>

      <ul class="aai-faq-list" role="list">
        {faqItems.map((item, i) => (
          <li class="aai-faq-item">
            <details>
              <summary>
                <span class="aai-faq-num">{String(i + 1).padStart(2, '0')}</span>
                <span class="aai-faq-q">{item.q}</span>
                <span class="aai-faq-chev" aria-hidden="true">+</span>
              </summary>
              <div class="aai-faq-a">
                <p>{item.a}</p>
              </div>
            </details>
          </li>
        ))}
      </ul>
    </section>

    <!-- ═══════ ФИНАЛЬНЫЙ CTA ═══════ -->
    <section class="aai-section aai-finale" id="aai-finale" aria-labelledby="aai-finale-h">
      <span class="aai-finale-glyph" aria-hidden="true">⁘</span>
      <h2 id="aai-finale-h" class="aai-finale-h">
        Чистка <em class="it">за один день</em> — без потери смысла
      </h2>
      <p class="aai-finale-lede">
        Оцените стоимость в калькуляторе или напишите в Telegram —
        предметно разберём работу, согласуем объём правок и сроки.
        Решаем такие задачи <em class="it">ежедневно с апреля&nbsp;2026</em>.
      </p>
      <div class="aai-finale-actions">
        <a href="/order?service=anti-ai" class="btn btn-gold btn-lg">
          Рассчитать — {entryHint}
        </a>
        <a href="https://t.me/academicsaloon" target="_blank" rel="noopener" class="btn btn-ghost-gold btn-lg">
          Написать в Telegram
        </a>
      </div>
      <p class="aai-finale-tag">Академический Салон · мастерская с 2020</p>
    </section>

  </article>

</Base>

<style is:global>
  /* ════════════════════════════════════════════════════════════════
     /anti-ai — Editorial dark landing.
     Все цвета / шрифты / интервалы — токены из design-tokens.css.
     Префикс .aai-* чтобы не конфликтовать с другими страницами.
     ════════════════════════════════════════════════════════════════ */
  .aai {
    color: var(--cream);
    overflow-x: hidden;
  }

  /* ───────── HERO ───────── */
  .aai .aai-hero {
    position: relative;
    max-width: var(--wrap-narrow);
    margin: 0 auto;
    padding: clamp(64px, 10vw, 140px) clamp(20px, 4vw, 48px) clamp(80px, 12vw, 160px);
    display: flex;
    flex-direction: column;
    gap: clamp(24px, 3vw, 40px);
  }

  /* Eyebrow — gold rule + label */
  .aai .aai-eyebrow {
    display: inline-flex;
    align-items: center;
    gap: 14px;
    padding: 0;
  }
  .aai .aai-eyebrow .rule {
    width: 28px;
    height: 1px;
    background: var(--gold-ref);
    display: inline-block;
    flex-shrink: 0;
  }
  .aai .aai-eyebrow .text {
    font-family: var(--fm);
    font-size: 11px;
    letter-spacing: .22em;
    text-transform: uppercase;
    color: var(--gold-ref);
  }

  /* 3-column header — ритм фактов под eyebrow */
  .aai .aai-head {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: clamp(20px, 4vw, 56px);
    border-top: 1px solid var(--line-ref);
    border-bottom: 1px solid var(--line-ref);
    padding: clamp(16px, 2vw, 22px) 0;
  }
  .aai .aai-head .col {
    display: flex;
    flex-direction: column;
    gap: 6px;
  }
  .aai .aai-head .lbl {
    font-family: var(--fm);
    font-size: 10px;
    letter-spacing: .22em;
    text-transform: uppercase;
    color: var(--mute);
  }
  .aai .aai-head .val {
    font-family: var(--fd);
    font-weight: 400;
    font-size: clamp(20px, 2.4vw, 28px);
    line-height: 1.1;
    color: var(--cream);
    letter-spacing: -.005em;
    font-feature-settings: "tnum";
  }
  .aai .aai-head .val .it {
    font-style: italic;
    color: var(--gold-ref);
    font-feature-settings: "swsh", "dlig";
  }

  /* H1 — Cormorant с italic-em золотом, в ритме главной (.prelude-h) */
  .aai .aai-h {
    font-family: var(--fd);
    font-weight: 400;
    font-size: clamp(36px, 5.2vw, 76px);
    line-height: 1.05;
    letter-spacing: -.022em;
    color: var(--cream);
    margin: 0;
    text-wrap: balance;
  }
  .aai .aai-h .ln,
  .aai .aai-h .ln-2 {
    display: block;
  }
  .aai .aai-h .ln-2 {
    margin-top: clamp(4px, .6vw, 10px);
  }
  .aai .aai-h em.it,
  .aai .aai-h .it {
    font-style: italic;
    color: var(--gold-ref);
    font-feature-settings: "swsh", "dlig", "liga";
    font-weight: 400;
  }

  /* Lede — italic Cormorant, узкая колонка */
  .aai .aai-lede {
    font-family: var(--fd);
    font-style: italic;
    font-size: clamp(17px, 1.6vw, 22px);
    line-height: 1.55;
    color: var(--cream-2);
    max-width: 56ch;
    margin: 0;
    font-feature-settings: "swsh", "dlig";
  }

  /* Actions — gold + ghost, large */
  .aai .aai-actions {
    display: flex;
    flex-wrap: wrap;
    gap: 14px 16px;
    margin-top: clamp(8px, 1vw, 16px);
  }
  .aai .aai-actions .btn {
    min-height: 56px;
  }

  /* Scroll cue — тонкая золотая линия с подписью */
  .aai .aai-scroll-cue {
    margin-top: clamp(40px, 6vw, 72px);
    display: inline-flex;
    flex-direction: column;
    align-items: flex-start;
    gap: 10px;
    text-decoration: none;
    color: var(--mute);
    font-family: var(--fm);
    font-size: 10px;
    letter-spacing: .22em;
    text-transform: uppercase;
    transition: color .35s var(--ease-silk);
  }
  .aai .aai-scroll-cue:hover { color: var(--gold-ref); }
  .aai .aai-scroll-cue .line {
    width: 1px;
    height: clamp(40px, 5vw, 64px);
    background: linear-gradient(180deg, var(--gold-ref), transparent);
    display: inline-block;
  }

  /* ───────── SECTIONS — общий контейнер ───────── */
  .aai .aai-section {
    max-width: var(--wrap-narrow);
    margin: 0 auto;
    padding: clamp(64px, 9vw, 120px) clamp(20px, 4vw, 48px);
    border-top: 1px solid var(--line-ref);
  }

  /* ───────── Chapter head — общий заголовок секций ───────── */
  .aai .aai-chapter-head {
    display: grid;
    grid-template-columns: clamp(80px, 12vw, 140px) 1fr;
    gap: clamp(20px, 4vw, 56px);
    align-items: start;
    margin-bottom: clamp(40px, 5vw, 64px);
  }
  .aai .aai-chapter-num {
    font-family: var(--fd);
    font-style: italic;
    font-weight: 400;
    font-size: clamp(56px, 7vw, 88px);
    line-height: .85;
    color: var(--gold-ref);
    letter-spacing: -.02em;
    font-feature-settings: "swsh", "onum";
  }
  .aai .aai-chapter-body {
    display: flex;
    flex-direction: column;
    gap: 10px;
    min-width: 0;
  }
  .aai .aai-chapter-eyebrow {
    font-family: var(--fm);
    font-size: 11px;
    letter-spacing: .22em;
    text-transform: uppercase;
    color: var(--mute);
  }
  .aai .aai-chapter-h {
    font-family: var(--fd);
    font-weight: 400;
    font-size: clamp(28px, 4vw, 56px);
    line-height: 1.05;
    letter-spacing: -.02em;
    color: var(--cream);
    margin: 0;
    text-wrap: balance;
  }
  .aai .aai-chapter-h em.it,
  .aai .aai-chapter-h .it {
    font-style: italic;
    color: var(--gold-ref);
    font-feature-settings: "swsh", "dlig";
    font-weight: 400;
  }

  @media (max-width: 600px) {
    .aai .aai-chapter-head {
      grid-template-columns: 1fr;
      gap: 4px;
    }
    .aai .aai-chapter-num { font-size: clamp(42px, 14vw, 64px); }
  }

  /* ───────── I — БОЛЬ ───────── */
  .aai .aai-pain-quotes {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: clamp(20px, 3vw, 36px);
  }
  .aai .aai-quote {
    margin: 0;
    padding: clamp(2px, 0.4vw, 8px) 0 clamp(2px, 0.4vw, 8px) clamp(20px, 2.5vw, 28px);
    border-left: 1px solid var(--gold-line);
    background: transparent;
    display: flex;
    flex-direction: column;
    gap: 14px;
  }
  .aai .aai-quote blockquote {
    margin: 0;
    font-family: var(--fd);
    font-style: italic;
    font-size: clamp(17px, 1.6vw, 21px);
    line-height: 1.45;
    color: var(--cream);
    font-feature-settings: "swsh", "dlig";
  }
  .aai .aai-quote blockquote::before {
    content: "«";
    color: var(--gold-ref);
    margin-right: 2px;
  }
  .aai .aai-quote blockquote::after {
    content: "»";
    color: var(--gold-ref);
    margin-left: 2px;
  }
  .aai .aai-quote figcaption {
    font-family: var(--fm);
    font-size: 10px;
    letter-spacing: .16em;
    text-transform: uppercase;
    color: var(--mute);
  }
  .aai .aai-pain-note {
    margin: clamp(32px, 4vw, 48px) 0 0;
    text-align: center;
    font-family: var(--fd);
    font-size: clamp(18px, 1.7vw, 22px);
    color: var(--cream-2);
  }
  .aai .aai-pain-note em.it,
  .aai .aai-pain-note .it {
    font-style: italic;
    color: var(--gold-ref);
  }
  @media (max-width: 800px) {
    .aai .aai-pain-quotes { grid-template-columns: 1fr; }
  }

  /* ───────── II — ПОЧЕМУ ТАК ───────── */
  .aai .aai-why-cols {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: clamp(28px, 3.5vw, 48px);
  }
  .aai .aai-why-col {
    display: flex;
    flex-direction: column;
    gap: 14px;
    border-top: 2px solid var(--gold-line);
    padding-top: 20px;
  }
  .aai .aai-why-col .aai-why-mark {
    font-family: var(--fd);
    font-style: italic;
    font-size: clamp(34px, 3.6vw, 52px);
    line-height: .9;
    color: var(--gold-ref);
    font-feature-settings: "swsh", "onum";
    margin: 0 0 -2px;
  }
  .aai .aai-why-col h3 {
    font-family: var(--fd);
    font-weight: 400;
    font-size: clamp(20px, 2vw, 26px);
    line-height: 1.15;
    color: var(--cream);
    letter-spacing: -.01em;
    margin: 0;
  }
  .aai .aai-why-col p {
    font-family: var(--fi);
    font-size: clamp(14px, 1.1vw, 16px);
    line-height: 1.6;
    color: var(--cream-2);
    margin: 0;
  }
  .aai .aai-why-col .aai-why-src {
    display: flex;
    flex-wrap: wrap;
    gap: 6px 18px;
    margin-top: auto;
    padding-top: 12px;
  }
  .aai .aai-why-col .aai-why-src a {
    font-family: var(--fm);
    font-size: 10px;
    letter-spacing: .16em;
    text-transform: uppercase;
    color: var(--gold-ref);
    text-decoration: none;
    border-bottom: 1px solid transparent;
    padding-bottom: 1px;
    transition: border-color .3s var(--ease-silk), color .3s var(--ease-silk);
  }
  .aai .aai-why-col .aai-why-src a:hover {
    border-bottom-color: var(--gold-line);
    color: var(--gold-hi);
  }
  @media (max-width: 800px) {
    .aai .aai-why-cols { grid-template-columns: 1fr; }
    .aai .aai-why-col .aai-why-src { margin-top: 4px; }
  }

  /* ───────── III — ЧТО ДЕЛАЕМ ───────── */
  .aai .aai-steps {
    list-style: none;
    margin: 0;
    padding: 0;
    display: flex;
    flex-direction: column;
  }
  .aai .aai-step {
    display: grid;
    grid-template-columns: clamp(64px, 9vw, 110px) 1fr;
    gap: clamp(20px, 3vw, 40px);
    padding: clamp(20px, 2.5vw, 32px) 0;
    border-top: 1px solid var(--line-ref);
    align-items: baseline;
  }
  .aai .aai-step:first-child { border-top: 0; padding-top: 0; }
  .aai .aai-step-n {
    font-family: var(--fd);
    font-style: italic;
    font-weight: 400;
    font-size: clamp(40px, 5vw, 64px);
    line-height: 1;
    color: var(--gold-ref);
    letter-spacing: -.02em;
    font-feature-settings: "swsh";
  }
  .aai .aai-step-body {
    display: flex;
    flex-direction: column;
    gap: 8px;
    max-width: 60ch;
  }
  .aai .aai-step-body h3 {
    font-family: var(--fd);
    font-weight: 400;
    font-size: clamp(22px, 2.4vw, 32px);
    line-height: 1.15;
    letter-spacing: -.01em;
    color: var(--cream);
    margin: 0;
  }
  .aai .aai-step-body p {
    font-family: var(--fi);
    font-size: clamp(15px, 1.2vw, 17px);
    line-height: 1.6;
    color: var(--cream-2);
    margin: 0;
  }

  /* ───────── IV — ЦЕНА ───────── */
  .aai .aai-pricing-table-wrap {
    overflow-x: auto;
    -webkit-overflow-scrolling: touch;
    margin: 0 calc(-1 * clamp(20px, 4vw, 48px));
    padding: 0 clamp(20px, 4vw, 48px);
  }
  .aai .aai-pricing-table {
    width: 100%;
    border-collapse: collapse;
    min-width: 640px;
    font-family: var(--fi);
  }
  .aai .aai-pricing-table th,
  .aai .aai-pricing-table td {
    padding: clamp(12px, 1.4vw, 18px) clamp(10px, 1.2vw, 16px);
    text-align: center;
    vertical-align: baseline;
    border-bottom: 1px solid var(--line-ref);
  }
  .aai .aai-pricing-table thead th {
    border-bottom: 1px solid var(--gold-line);
    text-align: center;
  }
  .aai .aai-pricing-table thead th .th-l {
    display: block;
    font-family: var(--fm);
    font-size: 11px;
    letter-spacing: .18em;
    text-transform: uppercase;
    color: var(--cream);
    margin-bottom: 4px;
  }
  .aai .aai-pricing-table thead th .th-s {
    display: block;
    font-family: var(--fd);
    font-style: italic;
    font-size: 13px;
    color: var(--gold-ref);
    font-feature-settings: "tnum";
  }
  .aai .aai-pricing-table .aai-pricing-corner {
    border-bottom: 1px solid var(--gold-line);
    background: transparent;
  }
  .aai .aai-pricing-table .aai-pricing-row-h {
    text-align: left;
    border-right: 1px solid var(--line-ref);
    padding-right: clamp(16px, 2vw, 24px);
    min-width: 220px;
  }
  .aai .aai-pricing-table .aai-pricing-row-h .rh-l {
    display: block;
    font-family: var(--fd);
    font-size: clamp(15px, 1.3vw, 18px);
    color: var(--cream);
    line-height: 1.25;
    margin-bottom: 4px;
  }
  .aai .aai-pricing-table .aai-pricing-row-h .rh-s {
    display: block;
    font-family: var(--fm);
    font-size: 10px;
    letter-spacing: .16em;
    text-transform: uppercase;
    color: var(--mute);
    font-feature-settings: "tnum";
  }
  .aai .aai-pricing-table .aai-pricing-cell {
    display: block;
    font-family: var(--fd);
    font-size: clamp(16px, 1.4vw, 20px);
    color: var(--cream);
    text-decoration: none;
    font-feature-settings: "tnum", "onum";
    padding: 6px 0;
    transition: color .3s var(--ease-silk), background .3s var(--ease-silk);
    position: relative;
    line-height: 1.2;
  }
  .aai .aai-pricing-table .aai-pricing-cell::after {
    content: "→";
    display: inline-block;
    margin-left: 8px;
    font-style: italic;
    color: var(--gold-ref);
    opacity: 0;
    transform: translateX(-4px);
    transition: opacity .3s var(--ease-silk), transform .3s var(--ease-silk);
  }
  .aai .aai-pricing-table .aai-pricing-cell:hover {
    color: var(--gold-ref);
  }
  .aai .aai-pricing-table .aai-pricing-cell:hover::after {
    opacity: 1;
    transform: translateX(0);
  }
  .aai .aai-pricing-table .aai-pricing-na {
    color: var(--mute-2);
    font-family: var(--fd);
    font-style: italic;
    font-size: clamp(16px, 1.4vw, 20px);
    cursor: not-allowed;
  }
  .aai .aai-pricing-note {
    margin: clamp(24px, 3vw, 36px) 0 0;
    font-family: var(--fd);
    font-size: clamp(14px, 1.1vw, 16px);
    line-height: 1.55;
    color: var(--mute);
    max-width: 64ch;
  }
  .aai .aai-pricing-note em.it,
  .aai .aai-pricing-note .it {
    font-style: italic;
    color: var(--gold-ref);
  }

  /* ───────── V — FAQ ───────── */
  .aai .aai-faq-list {
    list-style: none;
    margin: 0;
    padding: 0;
    display: flex;
    flex-direction: column;
  }
  .aai .aai-faq-item {
    border-top: 1px solid var(--line-ref);
  }
  .aai .aai-faq-item:last-child { border-bottom: 1px solid var(--line-ref); }
  .aai .aai-faq-item details { margin: 0; }
  .aai .aai-faq-item summary {
    list-style: none;
    cursor: pointer;
    display: grid;
    grid-template-columns: clamp(40px, 6vw, 64px) 1fr auto;
    gap: clamp(14px, 2vw, 28px);
    align-items: baseline;
    padding: clamp(18px, 2vw, 24px) 0;
    transition: color .3s var(--ease-silk);
  }
  .aai .aai-faq-item summary::-webkit-details-marker { display: none; }
  .aai .aai-faq-item summary:hover { color: var(--gold-ref); }
  .aai .aai-faq-item summary:hover .aai-faq-num,
  .aai .aai-faq-item summary:hover .aai-faq-chev {
    color: var(--gold-hi);
  }
  .aai .aai-faq-num {
    font-family: var(--fm);
    font-size: 11px;
    letter-spacing: .18em;
    color: var(--mute);
    font-feature-settings: "tnum";
  }
  .aai .aai-faq-q {
    font-family: var(--fd);
    font-weight: 400;
    font-size: clamp(18px, 1.7vw, 22px);
    line-height: 1.3;
    color: var(--cream);
    letter-spacing: -.005em;
  }
  .aai .aai-faq-chev {
    font-family: var(--fd);
    font-style: italic;
    font-size: clamp(22px, 2.4vw, 30px);
    line-height: 1;
    color: var(--gold-ref);
    transition: transform .4s var(--ease-silk);
  }
  .aai .aai-faq-item details[open] .aai-faq-chev { transform: rotate(45deg); }
  .aai .aai-faq-a {
    padding: 0 0 clamp(20px, 2.4vw, 28px) calc(clamp(40px, 6vw, 64px) + clamp(14px, 2vw, 28px));
  }
  .aai .aai-faq-a p {
    font-family: var(--fd);
    font-size: clamp(15px, 1.3vw, 18px);
    line-height: 1.6;
    color: var(--cream-2);
    max-width: 64ch;
    margin: 0;
  }

  @media (max-width: 600px) {
    .aai .aai-faq-item summary {
      grid-template-columns: auto 1fr auto;
      gap: 12px;
    }
    .aai .aai-faq-a {
      padding-left: 0;
    }
  }

  /* ───────── ФИНАЛЬНЫЙ CTA ───────── */
  .aai .aai-finale {
    text-align: center;
    border-top: 1px solid var(--gold-line);
    background:
      radial-gradient(ellipse 80% 60% at 50% 0%, var(--gold-soft), transparent 70%);
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: clamp(16px, 2vw, 28px);
  }
  .aai .aai-finale-glyph {
    font-family: var(--fd);
    font-style: italic;
    font-size: clamp(40px, 5vw, 64px);
    line-height: 1;
    color: var(--gold-ref);
    letter-spacing: .15em;
    font-feature-settings: "swsh";
  }
  .aai .aai-finale-h {
    font-family: var(--fd);
    font-weight: 400;
    font-size: clamp(32px, 4.5vw, 60px);
    line-height: 1.05;
    letter-spacing: -.02em;
    color: var(--cream);
    margin: 0;
    text-wrap: balance;
    max-width: 22ch;
  }
  .aai .aai-finale-h em.it,
  .aai .aai-finale-h .it {
    font-style: italic;
    color: var(--gold-ref);
    font-feature-settings: "swsh", "dlig";
  }
  .aai .aai-finale-lede {
    font-family: var(--fd);
    font-style: italic;
    font-size: clamp(17px, 1.5vw, 21px);
    line-height: 1.55;
    color: var(--cream-2);
    max-width: 52ch;
    margin: 0;
  }
  .aai .aai-finale-lede em.it,
  .aai .aai-finale-lede .it {
    color: var(--gold-ref);
    font-style: italic;
  }
  .aai .aai-finale-actions {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: 14px 16px;
    margin-top: clamp(8px, 1vw, 16px);
  }
  .aai .aai-finale-actions .btn { min-height: 56px; }
  .aai .aai-finale-tag {
    margin: clamp(28px, 4vw, 48px) 0 0;
    font-family: var(--fm);
    font-size: 10px;
    letter-spacing: .22em;
    text-transform: uppercase;
    color: var(--mute);
  }

  @media (max-width: 600px) {
    .aai .aai-finale-actions { flex-direction: column; align-items: stretch; width: 100%; }
    .aai .aai-finale-actions .btn { width: 100%; justify-content: center; }
  }

  /* ───────── HERO mobile (existing) ───────── */
  @media (max-width: 600px) {
    .aai .aai-head {
      grid-template-columns: 1fr;
      gap: 14px;
    }
    .aai .aai-head .col {
      flex-direction: row;
      justify-content: space-between;
      align-items: baseline;
      padding: 6px 0;
      border-bottom: 1px dashed var(--line-ref);
    }
    .aai .aai-head .col:last-child { border-bottom: 0; }
    .aai .aai-actions { flex-direction: column; align-items: stretch; }
    .aai .aai-actions .btn { width: 100%; justify-content: center; }
  }
</style>
