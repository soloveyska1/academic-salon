---
import ClientRouter from 'astro/components/ClientRouter.astro';
import Nav from '../components/Nav.astro';
import Footer from '../components/Footer.astro';

import '../styles/design-tokens.css';
import '../styles/components.css';
import '../styles/couture.css';
import '../styles/nav.css';

interface Props {
  title?: string;
  description?: string;
  currentPath?: string;
  ogTitle?: string;
  ogDescription?: string;
  /** Page-specific JSON-LD blocks (Article, ItemList, BreadcrumbList, etc.). */
  extraSchema?: Record<string, unknown> | Array<Record<string, unknown>>;
  /** Emit <meta name="robots" content="noindex"> for fallback / private routes. */
  noindex?: boolean;
}

const {
  title = 'Библиотека академического салона — студенческие работы',
  description = 'Библиотека академического салона: 235+ бесплатных документов по психологии, социальной работе. Курсовые, ВКР, рефераты — скачивай или закажи индивидуально.',
  currentPath = Astro.url.pathname,
  ogTitle,
  ogDescription,
  extraSchema,
  noindex = false,
} = Astro.props;

const isHomePage = currentPath === '/';
const extraSchemaList = extraSchema
  ? (Array.isArray(extraSchema) ? extraSchema : [extraSchema])
  : [];
---

<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover" />
  <title>{title}</title>
  <meta name="description" content={description} />
  {noindex && <meta name="robots" content="noindex, nofollow" />}
  <meta property="og:title" content={ogTitle || title} />
  <meta property="og:description" content={ogDescription || description} />
  <meta property="og:type" content="website" />
  <meta property="og:locale" content="ru_RU" />
  <meta name="theme-color" content="#d4af37" />
  <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
  <link rel="apple-touch-icon" href="/apple-touch-icon.svg" />
  <link rel="manifest" href="/manifest.json" />
  <meta name="apple-mobile-web-app-capable" content="yes" />
  <meta name="apple-mobile-web-app-status-bar-style" content="black-translucent" />
  <meta name="apple-mobile-web-app-title" content="Библиотека АС" />
  <meta name="mobile-web-app-capable" content="yes" />
  <meta property="og:image" content="https://bibliosaloon.ru/og-image.png" />
  <meta property="og:url" content={`https://bibliosaloon.ru${Astro.url.pathname}`} />
  <meta name="twitter:card" content="summary_large_image" />
  <meta name="twitter:title" content={ogTitle || title} />
  <meta name="twitter:description" content={ogDescription || description} />
  <link rel="canonical" href={`https://bibliosaloon.ru${Astro.url.pathname}`} />

  <!--
    Yandex.Metrika — gated by cookie-consent banner (152-ФЗ compliance).
    The ym() queue is always created so existing track() / web-vitals calls
    are no-ops until consent. The actual tag.js is only fetched once the
    visitor accepts cookies via the banner.
  -->
  <script is:inline>
    (function() {
      window.ym = window.ym || function() { (window.ym.a = window.ym.a || []).push(arguments); };
      window.ym.l = 1 * new Date();

      function loadMetrika() {
        if (window.__salonMetrikaLoaded) return;
        window.__salonMetrikaLoaded = true;
        var d = document, s = "script", r = "https://mc.yandex.ru/metrika/tag.js";
        for (var j = 0; j < d.scripts.length; j++) { if (d.scripts[j].src === r) return; }
        var k = d.createElement(s), a = d.getElementsByTagName(s)[0];
        k.async = 1; k.src = r; a.parentNode.insertBefore(k, a);
        window.ym(108363627, "init", { clickmap: true, trackLinks: true, accurateTrackBounce: true, webvisor: true });
      }

      var cc = null;
      try { cc = localStorage.getItem('salon:cc'); } catch (_) {}
      if (cc === 'all') loadMetrika();

      window.__salonAcceptCookies = function() {
        try { localStorage.setItem('salon:cc', 'all'); localStorage.setItem('salon:cc:at', String(Date.now())); } catch (_) {}
        loadMetrika();
      };
      window.__salonRejectCookies = function() {
        try { localStorage.setItem('salon:cc', 'necessary'); localStorage.setItem('salon:cc:at', String(Date.now())); } catch (_) {}
      };
    })();
  </script>

  <!-- Core Web Vitals → Yandex.Metrika goals (deferred behind requestIdleCallback) -->
  <script>
    import { bootWebVitals } from '../scripts/web-vitals';

    function start() { bootWebVitals().catch(() => {}); }
    if ('requestIdleCallback' in window) {
      window.requestIdleCallback(start, { timeout: 2000 });
    } else {
      window.setTimeout(start, 1500);
    }
  </script>

  <script type="application/ld+json" set:html={JSON.stringify({
    "@context": "https://schema.org",
    "@type": "EducationalOrganization",
    "name": "Библиотека академического салона",
    "url": "https://bibliosaloon.ru",
    "description": "Библиотека студенческих работ: психология, социальная работа, конфликтология. Бесплатный каталог и индивидуальные заказы.",
    "sameAs": [
      "https://vk.com/academicsaloon",
      "https://t.me/academicsaloon"
    ],
    "contactPoint": {
      "@type": "ContactPoint",
      "email": "academsaloon@mail.ru",
      "contactType": "customer service"
    }
  })} />
  {extraSchemaList.map((schema) => (
    <script type="application/ld+json" set:html={JSON.stringify(schema)} />
  ))}
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link rel="dns-prefetch" href="https://fonts.gstatic.com" />
  <!-- Fonts — preload stylesheet (async), display=swap so fallback paints immediately -->
  <link rel="preload" as="style" href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,300;0,400;0,500;0,600;0,700;1,300;1,400;1,500;1,600;1,700&family=Inter:wght@300..700&family=Inter+Tight:wght@300;400;500;600&family=JetBrains+Mono:wght@300;400;500&display=swap" />
  <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,300;0,400;0,500;0,600;0,700;1,300;1,400;1,500;1,600;1,700&family=Inter:wght@300..700&family=Inter+Tight:wght@300;400;500;600&family=JetBrains+Mono:wght@300;400;500&display=swap" />
  <!-- Apply saved theme + bg before first paint (no hide-until-loaded — browser blocks render on external CSS anyway) -->
  <script is:inline>
    (function() {
      var root = document.documentElement;
      var t = null;
      try { t = localStorage.getItem('theme'); } catch (_) {}
      if (t === 'light') {
        root.setAttribute('data-theme', 'light');
        root.style.backgroundColor = '#faf8f2';
        root.style.colorScheme = 'light';
      } else {
        root.style.backgroundColor = '#09080a';
        root.style.colorScheme = 'dark';
      }
    })();
  </script>
  <!-- Capture click position for circle-reveal view transition -->
  <script is:inline>
    document.addEventListener('click', function(e) {
      var link = e.target.closest('a[href]');
      if (!link) return;
      var href = link.getAttribute('href');
      if (!href || href.startsWith('#') || href.startsWith('http') || href.startsWith('mailto')) return;
      var x = ((e.clientX / window.innerWidth) * 100).toFixed(1) + '%';
      var y = ((e.clientY / window.innerHeight) * 100).toFixed(1) + '%';
      document.documentElement.style.setProperty('--vt-x', x);
      document.documentElement.style.setProperty('--vt-y', y);
      if (window.__setAcademicSalonRouteTarget) {
        try {
          var path = new URL(href, window.location.origin).pathname;
          var label = (link.textContent || '').replace(/\s+/g, ' ').trim();
          window.__setAcademicSalonRouteTarget(path, label);
        } catch (_) {}
      }
    });
  </script>
  <!-- Critical inline CSS — prevents FOUC by rendering layout before external CSS loads -->
  <style is:inline>
    /* Metric-matched fallbacks — prevent reflow when webfonts swap in */
    @font-face{
      font-family:'Cormorant Fallback';
      src:local('Georgia'),local('Times New Roman'),local('Times');
      size-adjust:106%;
      ascent-override:95%;
      descent-override:28%;
      line-gap-override:0%;
    }
    @font-face{
      font-family:'Inter Fallback';
      src:local('Arial'),local('Helvetica Neue'),local('Helvetica');
      size-adjust:107%;
      ascent-override:90%;
      descent-override:22%;
      line-gap-override:0%;
    }

    /* Design tokens — minimal subset so first paint has colours, fonts, spacing */
    :root{
      --bg:#09080a;--bg2:#0c0b0d;--bg3:#0f0e10;
      --t1:#f0ede8;--t2:#9b9590;--t3:#656060;
      --ac:#d4af37;--ac2:#ecc94b;--ac3:#f6e27a;
      --brd:rgba(255,255,255,.06);
      --fd:'Cormorant Garamond','Cormorant Fallback',Georgia,serif;
      --fi:Inter,'Inter Tight','Inter Fallback',system-ui,sans-serif;
      --fm:'JetBrains Mono',ui-monospace,'SF Mono',Menlo,monospace;
      --nav-h:72px;
      --dur:.3s;
      --ease-ios:cubic-bezier(.25,0,.2,1);
      color-scheme:dark;
    }
    [data-theme="light"]{
      --bg:#faf8f2;--bg2:#f2efe8;--bg3:#ece9e1;
      --t1:#1a1610;--t2:#4a4540;--t3:#7a736c;
      --brd:rgba(0,0,0,.06);
      color-scheme:light;
    }

    /* First-paint bg on html + body — no white/grey flash */
    html,body{background:var(--bg);color:var(--t1)}
    *,*::before,*::after{box-sizing:border-box;margin:0;padding:0}
    body{font-family:var(--fi);overflow-x:clip;-webkit-font-smoothing:antialiased;-moz-osx-font-smoothing:grayscale}
    /* Headings inherit the display font but leave sizes to page-specific CSS — no forced snap */
    h1,h2,h3,h4,h5{font-family:var(--fd);font-weight:400;color:var(--t1)}
    button,input,textarea,select{font:inherit;color:inherit}

    a{color:inherit;text-decoration:none}
    /* Nav */
    .nav{position:fixed;top:0;left:0;right:0;z-index:100;height:72px;display:flex;align-items:center;justify-content:space-between;padding:0 48px;background:transparent;border-bottom:1px solid rgba(255,255,255,.06)}
    .nav-links{display:flex;align-items:center;gap:40px}
    .nav-logo{text-decoration:none;display:flex;align-items:center;gap:12px}
    .nav-actions{display:flex;align-items:center;gap:16px}
    .nav-burger{display:none}
    /* Main */
    .main{position:relative;z-index:1;padding-top:72px;min-height:100vh}
    /* Decorative — hidden until CSS loads */
    .film-grain,.scroll-progress,.bg-mesh,.hero-canvas{opacity:0}
    /* Mobile sheet primitives (bottom-nav styles live in Nav.astro / nav.css) */
    .bottom-sheet{position:fixed;bottom:0;left:0;right:0;z-index:99;transform:translateY(100%);pointer-events:none}
    .bottom-sheet.is-open{transform:translateY(0);pointer-events:auto}
    .sheet-backdrop{position:fixed;inset:0;z-index:98;opacity:0;pointer-events:none}
    .sheet-backdrop.is-open{pointer-events:auto}
    /* Mobile */
    @media(max-width:768px){
      .nav{padding:env(safe-area-inset-top,0px) max(20px,env(safe-area-inset-right,0px)) 0 max(20px,env(safe-area-inset-left,0px));height:calc(56px + env(safe-area-inset-top,0px))}
      .nav-links{display:none}
      .nav-burger{display:flex;flex-direction:column;justify-content:center;align-items:center;width:48px;height:48px;border:none;background:transparent;gap:8px}
      .burger-line{display:block;width:20px;height:1.5px;background:#f0ede8;border-radius:1px}
      .main{padding-top:calc(56px + env(safe-area-inset-top,0px));padding-bottom:calc(64px + env(safe-area-inset-bottom,0px))}
      [data-theme="light"] .burger-line{background:#1a1610}
      /* Hide sheet content until CSS loads — prevents raw link flash */
      .sheet-link,.sheet-contact,.sheet-reading,.sheet-divider,.sheet-contacts{opacity:0}
      .bottom-sheet.is-open .sheet-link,.bottom-sheet.is-open .sheet-contact,.bottom-sheet.is-open .sheet-reading,.bottom-sheet.is-open .sheet-contacts{opacity:1}
    }
  </style>
  {isHomePage && (
    <style is:inline>
      .hero{position:relative;min-height:100svh;margin-top:-72px;padding:72px 24px 0;display:flex;flex-direction:column;align-items:center;justify-content:center;overflow:hidden;background:#09080a}
      [data-theme="light"] .hero{background:#faf8f2}
      .hero-canvas{position:absolute;inset:0;width:100%;height:100%;pointer-events:none;z-index:0}
      .hero-glow{position:absolute;width:min(1200px,100vw);height:min(1000px,85vw);top:42%;left:50%;transform:translate(-50%,-50%);pointer-events:none;z-index:0;background:radial-gradient(ellipse 60% 50% at 50% 50%,rgba(200,165,40,.18) 0%,transparent 70%),radial-gradient(ellipse 40% 35% at 50% 45%,rgba(212,175,55,.1) 0%,transparent 55%)}
      .hero-inner{position:relative;z-index:1;max-width:960px;text-align:center;padding:0 8px}
      .hero-eyebrow{margin-bottom:24px;font:400 clamp(11px,1.3vw,12px)/1 Inter,system-ui,sans-serif;letter-spacing:.18em;text-transform:uppercase;color:#9b9590}
      .hero-title{font:300 clamp(48px,10vw,100px)/.88 'Cormorant Garamond',Georgia,serif;color:#f0ede8;letter-spacing:-.02em;text-wrap:balance}
      [data-theme="light"] .hero-title{color:#1a1610}
      .word{display:inline-block}
      .hero-rule{width:48px;height:1px;margin:32px auto 0;background:#d4af37;opacity:.25}
      .hero-sub{max-width:52ch;margin:28px auto 0;font:400 clamp(14px,1.8vw,16px)/1.65 Inter,system-ui,sans-serif;color:#9b9590;letter-spacing:.04em;word-break:break-word;overflow-wrap:break-word}
      .hero-actions{margin-top:48px;display:flex;gap:16px;justify-content:center;flex-wrap:wrap}
      .hero .btn{display:inline-flex;align-items:center;justify-content:center;min-height:48px;padding:16px 40px;border:1px solid rgba(212,175,55,.2);color:#f0ede8;text-transform:uppercase;letter-spacing:.12em;font:400 12px/1 Inter,system-ui,sans-serif;border-radius:0}
      .hero .btn--primary{background:rgba(255,255,255,.03)}
      .hero .btn--ghost{background:transparent;border-color:rgba(212,175,55,.12);color:#9b9590}
      [data-theme="light"] .hero .btn{color:#1a1610}
      .hero-metrics{position:absolute;left:24px;right:24px;bottom:80px;display:flex;justify-content:center;gap:80px;flex-wrap:wrap;z-index:1}
      .metric{text-align:center}
      .metric-value{display:block;font:300 clamp(42px,8vw,68px)/1 'JetBrains Mono',monospace;color:#d4af37;letter-spacing:-.02em;font-variant-numeric:tabular-nums}
      .metric-label{display:block;margin-top:12px;font:400 11px/1.3 Inter,system-ui,sans-serif;color:#656060;text-transform:uppercase;letter-spacing:.12em}
      @media(max-width:639px){
        .hero{min-height:75svh;padding:56px 16px calc(64px + env(safe-area-inset-bottom,0px))}
        .hero-title{font-size:clamp(38px,10vw,52px);line-height:1.1}
        .hero-actions{margin-top:32px;flex-direction:column;align-items:stretch;gap:8px}
        .hero .btn{text-align:center;padding:16px 32px;font-size:13px}
        .hero-metrics{position:relative;left:auto;right:auto;bottom:auto;margin-top:32px;gap:32px;padding:0 16px}
        .metric-value{font-size:clamp(32px,10vw,48px)}
      }
    </style>
  )}
  <ClientRouter />
</head>
<body>
  <a href="#main" class="skip-link">Перейти к содержимому</a>

  <!-- curtain removed — browser's render-blocking CSS handles first paint -->

  <!-- custom cursor removed — interferes with usability -->

  <!-- ═══════ FILM GRAIN ═══════ -->
  <div class="film-grain" aria-hidden="true"></div>

  <!-- ═══════ SCROLL PROGRESS ═══════ -->
  <div class="scroll-progress" id="scrollProgress" aria-hidden="true"></div>

  <!-- Background aurora mesh -->
  <div class="bg-mesh" aria-hidden="true">
    <div class="orb orb-1"></div>
    <div class="orb orb-2"></div>
    <div class="orb orb-3"></div>
  </div>

  <Nav currentPath={currentPath} />

  <main class="main" id="main">
    <slot />
  </main>

  <Footer />

  <!-- Cookie consent banner (152-ФЗ). Hidden until the user has neither
       accepted nor rejected. Choice is stored in localStorage; once made,
       the banner does not reappear. Yandex.Metrika is loaded only when
       'all' is chosen (see head script). -->
  <aside id="cookie-banner" class="cookie-banner" role="dialog" aria-modal="false" aria-labelledby="cb-title" aria-describedby="cb-desc" hidden>
    <div class="cb-inner">
      <div class="cb-text">
        <p id="cb-title" class="cb-title">Cookies и аналитика</p>
        <p id="cb-desc" class="cb-desc">
          Сайт использует технические cookies и Яндекс.Метрику (РФ) для понимания, что востребовано в каталоге.
          Аналитика подключается только после вашего согласия.
          <a href="/privacy">Политика</a> · <a href="/consent">Согласие на ПДн</a>
        </p>
      </div>
      <div class="cb-actions">
        <button type="button" class="cb-btn cb-btn--ghost" id="cb-reject">Только необходимые</button>
        <button type="button" class="cb-btn cb-btn--gold" id="cb-accept">Принять все</button>
      </div>
    </div>
  </aside>

  <script is:inline>
    (function() {
      var banner = document.getElementById('cookie-banner');
      if (!banner) return;
      var cc = null;
      try { cc = localStorage.getItem('salon:cc'); } catch (_) {}
      if (cc === 'all' || cc === 'necessary') return;
      banner.hidden = false;
      requestAnimationFrame(function() { banner.classList.add('is-open'); });

      function close() {
        banner.classList.remove('is-open');
        setTimeout(function() { banner.hidden = true; }, 280);
      }
      var acceptBtn = document.getElementById('cb-accept');
      var rejectBtn = document.getElementById('cb-reject');
      if (acceptBtn) acceptBtn.addEventListener('click', function() {
        if (window.__salonAcceptCookies) window.__salonAcceptCookies();
        close();
      });
      if (rejectBtn) rejectBtn.addEventListener('click', function() {
        if (window.__salonRejectCookies) window.__salonRejectCookies();
        close();
      });
    })();
  </script>

  <!-- Bottom navigation lives in <Nav /> (class .btm-nav) to avoid duplicate mobile bars -->

  <!-- Service worker registration -->
  <script is:inline>
    (function initServiceWorker() {
      if (!('serviceWorker' in navigator)) return;
      if (window.location.protocol !== 'https:' && window.location.hostname !== 'localhost') return;
      window.addEventListener('load', function() {
        var reloadedForNewWorker = false;
        navigator.serviceWorker.register('/sw.js', { scope: '/', updateViaCache: 'none' }).then(function(registration) {
          registration.update().catch(function() {});
          navigator.serviceWorker.addEventListener('controllerchange', function() {
            if (reloadedForNewWorker) return;
            reloadedForNewWorker = true;
            window.location.reload();
          });
        }).catch(function(err) {
          console.warn('SW registration failed', err);
        });
      }, { once: true });
    })();
  </script>

  <!-- Lenis Smooth Scroll CDN (async — non-blocking, survives SPA navs) -->
  <script is:inline>
    (function() {
      if (window.matchMedia('(pointer: coarse)').matches) return;
      if (window.matchMedia('(prefers-reduced-motion: reduce)').matches) return;

      var lenisScriptLoaded = false;
      var currentRafId = null;

      function startLenis() {
        if (typeof Lenis !== 'function') return;
        stopLenis();
        var lenis = new Lenis({
          duration: 1.2,
          easing: function(t) { return Math.min(1, 1.001 - Math.pow(2, -10 * t)) },
          touchMultiplier: 1,
          infinite: false,
        });
        function raf(time) { lenis.raf(time); currentRafId = requestAnimationFrame(raf); }
        currentRafId = requestAnimationFrame(raf);
        window.__lenis = lenis;
      }

      function stopLenis() {
        if (currentRafId != null) { cancelAnimationFrame(currentRafId); currentRafId = null; }
        if (window.__lenis && typeof window.__lenis.destroy === 'function') {
          try { window.__lenis.destroy(); } catch (_) {}
        }
        window.__lenis = null;
      }

      function ensureLenis() {
        if (lenisScriptLoaded) { startLenis(); return; }
        var s = document.createElement('script');
        s.src = 'https://unpkg.com/lenis@1.1.18/dist/lenis.min.js';
        s.onload = function() { lenisScriptLoaded = true; startLenis(); };
        s.onerror = function() { /* offline/CDN down — native scroll continues to work */ };
        document.head.appendChild(s);
      }

      ensureLenis();
      document.addEventListener('astro:before-swap', stopLenis);
      document.addEventListener('astro:page-load', ensureLenis);
    })();
  </script>

  <!-- Global Premium Effects -->
  <script is:inline>
    (function initGlobalEffects() {
      var win = window;

      /* Route shield removed — Astro View Transitions handle page transitions natively */
      win.__setAcademicSalonRouteTarget = function() {};
    })();

    // curtain / intro overlay — removed; trust native CSS render-blocking

    // ═══════ GOLD RIPPLE TOUCH EFFECT (mobile only) ═══════
    (function initRipple() {
      if (!window.matchMedia('(pointer: coarse)').matches) return;

      var SELECTORS = '.btn, .card, .chip, .pill, .faq-q, .btm-item, .guarantee, .gallery-more, .mobile-link, .attach-trigger';

      function createRipple(e) {
        var el = e.currentTarget;
        var rect = el.getBoundingClientRect();
        var size = Math.max(rect.width, rect.height) * 2;
        var x = (e.touches ? e.touches[0].clientX : e.clientX) - rect.left - size / 2;
        var y = (e.touches ? e.touches[0].clientY : e.clientY) - rect.top - size / 2;

        var circle = document.createElement('span');
        circle.className = 'ripple-circle';
        circle.style.width = circle.style.height = size + 'px';
        circle.style.left = x + 'px';
        circle.style.top = y + 'px';
        el.appendChild(circle);

        // Haptic feedback (Android)
        if (navigator.vibrate) navigator.vibrate(8);

        circle.addEventListener('animationend', function() { circle.remove(); });
      }

      function bind() {
        document.querySelectorAll(SELECTORS).forEach(function(el) {
          if (el.dataset.ripple) return;
          el.dataset.ripple = '1';
          el.addEventListener('touchstart', createRipple, { passive: true });
        });
      }

      bind();
      document.addEventListener('astro:page-load', bind);
    })();

    // ═══════ MAGNETIC BUTTONS — premium drift, amplitude scales with button size ═══════
    (function initMagnetic() {
      function bind() {
        if (window.matchMedia('(pointer: coarse)').matches) return;
        document.querySelectorAll('.btn, .btn-primary, .btn-gold, .btn-pill, .nav-cta, .float-cta-btn, .hero-path, .hero-cta, .done-link, .gallery-more').forEach(function(el) {
          if (el.dataset.magnetic) return;
          var rect = el.getBoundingClientRect();
          /* Larger buttons get smaller amplitude so they drift instead of jerking */
          var amp = rect.width > 240 ? 0.06 : (rect.width > 160 ? 0.09 : 0.12);
          el.dataset.magnetic = '1';
          el.addEventListener('mousemove', function(e) {
            var r = el.getBoundingClientRect();
            var x = e.clientX - r.left - r.width / 2;
            var y = e.clientY - r.top - r.height / 2;
            el.style.transform = 'translate(' + (x * amp).toFixed(2) + 'px,' + (y * amp).toFixed(2) + 'px)';
          });
          el.addEventListener('mouseleave', function() {
            el.style.transform = '';
            el.style.transition = 'transform .32s cubic-bezier(.22,.61,.36,1)';
            setTimeout(function() { el.style.transition = '' }, 340);
          });
        });
      }
      bind();
      document.addEventListener('astro:page-load', bind);
    })();

    // ═══════ SCROLL PROGRESS BAR (JS fallback — CSS handles it if supported) ═══════
    (function initScrollProgress() {
      if (CSS.supports && CSS.supports('animation-timeline', 'scroll()')) return;
      var bar = document.getElementById('scrollProgress');
      if (!bar) return;
      bar.style.width = '0%';
      bar.style.transform = 'none';
      window.addEventListener('scroll', function() {
        var h = document.documentElement.scrollHeight - window.innerHeight;
        var pct = h > 0 ? (window.scrollY / h) * 100 : 0;
        bar.style.width = pct + '%';
      }, { passive: true });
    })();

    // ═══════ GLOBAL RUNTIME KIT ═══════
    (function initRuntimeKit() {
      var win = window;
      var METRIKA_ID = 108363627;
      var app = win.academicSalon || {};

      function getToastContainer() {
        var container = document.getElementById('toast-container');
        if (!container) {
          container = document.createElement('div');
          container.id = 'toast-container';
          container.className = 'toast-container';
          container.setAttribute('aria-live', 'polite');
          container.setAttribute('aria-atomic', 'true');
          document.body.appendChild(container);
        }
        return container;
      }

      function showToast(msg, type, duration) {
        if (!msg) return;
        type = type || 'success';
        duration = duration || 3200;
        var container = getToastContainer();
        var toast = document.createElement('div');
        toast.className = 'toast toast--' + type;
        toast.setAttribute('role', type === 'error' ? 'alert' : 'status');
        toast.textContent = msg;
        container.appendChild(toast);
        setTimeout(function() {
          toast.classList.add('toast--exit');
          setTimeout(function() { toast.remove(); }, 300);
        }, duration);
      }

      function sanitizeGoal(name) {
        return String(name || 'event')
          .toLowerCase()
          .replace(/[^a-z0-9_]+/g, '_')
          .replace(/^_+|_+$/g, '')
          .slice(0, 64) || 'event';
      }

      function track(name, params) {
        var goal = sanitizeGoal(name);
        try {
          if (typeof win.ym === 'function') {
            win.ym(METRIKA_ID, 'reachGoal', goal, params || {});
          }
        } catch (_) {}
        try {
          document.dispatchEvent(new CustomEvent('academic-salon:track', {
            detail: { name: goal, params: params || {} }
          }));
        } catch (_) {}
      }

      function copyText(text) {
        if (navigator.clipboard && win.isSecureContext) {
          return navigator.clipboard.writeText(text);
        }
        return new Promise(function(resolve, reject) {
          try {
            var area = document.createElement('textarea');
            area.value = text;
            area.setAttribute('readonly', 'true');
            area.style.position = 'fixed';
            area.style.opacity = '0';
            area.style.inset = '0';
            document.body.appendChild(area);
            area.focus();
            area.select();
            var ok = document.execCommand('copy');
            area.remove();
            if (!ok) throw new Error('copy-failed');
            resolve();
          } catch (err) {
            reject(err);
          }
        });
      }

      function bindDraft(form, options) {
        options = options || {};
        if (!form || !options.key) {
          return { clear: function() {}, save: function() {}, restore: function() {} };
        }

        var storageKey = 'academic-salon:draft:' + options.key;
        var writeTimer = 0;

        function readRaw() {
          try {
            return JSON.parse(localStorage.getItem(storageKey) || 'null');
          } catch (_) {
            return null;
          }
        }

        function serialize() {
          var payload = {};
          form.querySelectorAll('input[name], textarea[name], select[name]').forEach(function(field) {
            if (!field.name || field.disabled || field.type === 'file' || field.type === 'password') return;
            if ((field.type === 'checkbox' || field.type === 'radio')) {
              if (field.checked) payload[field.name] = field.value;
              return;
            }
            payload[field.name] = field.value;
          });
          if (typeof options.getState === 'function') {
            payload.__extra = options.getState() || {};
          }
          return payload;
        }

        function hasContent(payload) {
          return Object.keys(payload).some(function(key) {
            if (key === '__extra') {
              return payload.__extra && Object.keys(payload.__extra).length;
            }
            var value = payload[key];
            if (Array.isArray(value)) return value.length > 0;
            return value != null && String(value).trim() !== '';
          });
        }

        function save() {
          var payload = serialize();
          if (!hasContent(payload)) {
            localStorage.removeItem(storageKey);
            return;
          }
          localStorage.setItem(storageKey, JSON.stringify({
            savedAt: Date.now(),
            values: payload
          }));
        }

        function scheduleSave() {
          clearTimeout(writeTimer);
          writeTimer = setTimeout(save, 180);
        }

        function restore() {
          var raw = readRaw();
          if (!raw || !raw.values) return false;
          var values = raw.values;
          form.querySelectorAll('input[name], textarea[name], select[name]').forEach(function(field) {
            if (!field.name || field.disabled || field.type === 'file' || field.type === 'password') return;
            if (!(field.name in values)) return;
            if (field.type === 'checkbox' || field.type === 'radio') {
              field.checked = String(values[field.name]) === String(field.value);
              return;
            }
            field.value = values[field.name];
          });
          if (typeof options.setState === 'function' && values.__extra) {
            options.setState(values.__extra);
          }
          if (!options.silent && hasContent(values)) {
            showToast('Черновик восстановлен', 'info', 2200);
            track(options.key + '_draft_restored');
          }
          return true;
        }

        form.addEventListener('input', scheduleSave, true);
        form.addEventListener('change', scheduleSave, true);

        return {
          clear: function() { localStorage.removeItem(storageKey); },
          save: save,
          restore: restore
        };
      }

      app.showToast = showToast;
      app.track = track;
      app.copyText = copyText;
      app.bindDraft = bindDraft;
      win.academicSalon = app;
      win.showToast = showToast;

      // Stage 58 — capture ?ref=CODE on first visit (any page),
      // store in sessionStorage so /order picks it up at submit
      // even if user navigates around. Cap at 6 chars, alphanum only.
      try {
        var qs = new URLSearchParams(win.location.search);
        var ref = (qs.get('ref') || '').toUpperCase().replace(/[^A-Z0-9]/g, '').slice(0, 6);
        if (ref) {
          var existing = '';
          try { existing = sessionStorage.getItem('salon:ref') || ''; } catch (_) {}
          if (existing !== ref) {
            try { sessionStorage.setItem('salon:ref', ref); } catch (_) {}
            try { sessionStorage.setItem('salon:ref:firstSeenAt', String(Math.floor(Date.now()/1000))); } catch (_) {}
            track('referral_seen', { code: ref });
          }
        }
      } catch (_) {}
    })();

    // ═══════ REVEAL OBSERVER ═══════
    (function initReveal() {
      var observer = new IntersectionObserver(
        function(entries) {
          entries.forEach(function(e) {
            if (e.isIntersecting) {
              e.target.classList.add('visible');
              observer.unobserve(e.target);
            }
          });
        },
        { threshold: 0.1, rootMargin: '0px 0px -40px 0px' }
      );
      document.querySelectorAll('.reveal').forEach(function(el) { observer.observe(el) });
    })();

    // Clean up before view transition swap
    document.addEventListener('astro:before-swap', function() {
      // Stop Lenis to prevent scroll jank during transition
      if (window.__lenis) window.__lenis.stop();
      // Hide decorative overlays
      var grain = document.querySelector('.film-grain');
      if (grain) grain.style.visibility = 'hidden';
      var progress = document.querySelector('.scroll-progress');
      if (progress) progress.style.visibility = 'hidden';
    });

    // Re-init after Astro view transitions
    document.addEventListener('astro:after-swap', function() {
      document.querySelectorAll('.reveal:not(.visible)').forEach(function(el) {
        new IntersectionObserver(function(entries, obs) {
          entries.forEach(function(e) {
            if (e.isIntersecting) { e.target.classList.add('visible'); obs.unobserve(e.target) }
          });
        }, { threshold: 0.1 }).observe(el);
      });

      // Hide decorative layers during transitions
      var curtain = document.getElementById('curtain');
      if (curtain) curtain.style.display = 'none';
      var grain = document.querySelector('.film-grain');
      if (grain) grain.style.visibility = 'hidden';
      var progress = document.querySelector('.scroll-progress');
      if (progress) progress.style.visibility = 'hidden';
      // Restore after transition completes
      setTimeout(function() {
        if (grain) grain.style.visibility = '';
        if (progress) progress.style.visibility = '';
      }, 300);

      // Magnetic buttons re-init handled by index.astro's own cleanup system
    });

    // ═══════ HIDDEN ADMIN ENTRY ═══════
    (function initAdminTrigger() {
      var clicks = 0;
      var lastClickAt = 0;
      document.addEventListener('click', function(event) {
        var target = event.target.closest('.footer-copy, .ftr-copy, .colophon, .end-copy');
        if (!target) return;
        var now = Date.now();
        if (now - lastClickAt > 2500) clicks = 0;
        lastClickAt = now;
        clicks += 1;
        if (clicks >= 7) {
          clicks = 0;
          window.location.href = '/admin';
        }
      });
    })();
  </script>
</body>
</html>

<style is:global>
  /* Background — Aurora Gradient */
  .bg-mesh {
    position: fixed; inset: 0;
    z-index: -1;
    pointer-events: none;
    overflow: hidden;
    opacity: 1; /* override inline FOUC hide */
  }
  [data-theme="light"] .bg-mesh { opacity: 0.5 }
  [data-theme="light"] .orb-1 { background: rgba(122,95,18, 0.04); }
  [data-theme="light"] .orb-2 { background: rgba(109,40,217, 0.025); }
  [data-theme="light"] .orb-3 { background: rgba(122,95,18, 0.03); }
  [data-theme="light"] .bg-mesh::after {
    background:
      radial-gradient(ellipse 80% 60% at 15% 20%, rgba(122,95,18,.03) 0%, transparent 70%),
      radial-gradient(ellipse 60% 50% at 85% 75%, rgba(109,40,217,.02) 0%, transparent 70%),
      radial-gradient(ellipse 50% 40% at 50% 50%, rgba(122,95,18,.015) 0%, transparent 60%);
  }

  .orb {
    position: absolute;
    border-radius: 50%;
    filter: blur(100px);
    will-change: transform;
  }
  .orb-1 {
    width: 700px; height: 700px;
    top: -15%; left: -10%;
    background: var(--ac-sh);
    animation: orb-float-1 25s ease-in-out infinite alternate;
  }
  .orb-2 {
    width: 500px; height: 500px;
    bottom: -15%; right: -10%;
    background: rgba(167,139,250,.035);
    animation: orb-float-2 22s ease-in-out infinite alternate;
  }
  .orb-3 {
    width: 600px; height: 600px;
    top: 40%; left: 50%;
    background: var(--ac-t);
    animation: orb-float-3 30s ease-in-out infinite alternate;
  }
  @keyframes orb-float-1 {
    0%   { transform: translate(0,0) scale(1) }
    33%  { transform: translate(80px,-80px) scale(1.15) }
    66%  { transform: translate(-40px,40px) scale(0.9) }
    100% { transform: translate(20px,-20px) scale(1.05) }
  }
  @keyframes orb-float-2 {
    0%   { transform: translate(0,0) scale(1) }
    33%  { transform: translate(-60px,-50px) scale(1.12) }
    66%  { transform: translate(30px,30px) scale(0.88) }
    100% { transform: translate(-10px,20px) scale(1.08) }
  }
  @keyframes orb-float-3 {
    0%   { transform: translate(-50%,-50%) scale(1) }
    50%  { transform: translate(calc(-50% + 100px), calc(-50% - 80px)) scale(1.2) }
    100% { transform: translate(calc(-50% - 60px), calc(-50% + 40px)) scale(0.85) }
  }

  /* Aurora overlay — subtle animated gradient mesh */
  .bg-mesh::after {
    content: '';
    position: absolute;
    inset: 0;
    background:
      radial-gradient(ellipse 80% 60% at 15% 20%, var(--ac-g2) 0%, transparent 70%),
      radial-gradient(ellipse 60% 50% at 85% 75%, rgba(167,139,250,.03) 0%, transparent 70%),
      radial-gradient(ellipse 50% 40% at 50% 50%, var(--ac-t) 0%, transparent 60%);
    animation: aurora-shift 20s ease-in-out infinite alternate;
  }
  @keyframes aurora-shift {
    0%   { opacity: 0.8; filter: hue-rotate(0deg); }
    50%  { opacity: 1; filter: hue-rotate(5deg); }
    100% { opacity: 0.7; filter: hue-rotate(-3deg); }
  }

  .main {
    position: relative;
    z-index: 1;
    padding-top: var(--nav-h);
    min-height: 100vh;
  }

  /* View transitions — editorial circle-reveal from click point (cinematic) */
  ::view-transition-group(root) {
    animation-duration: .46s;
    animation-timing-function: cubic-bezier(.4,0,.2,1);
  }
  ::view-transition-old(root) {
    animation: vt-fade-out .22s cubic-bezier(.4,0,.2,1) forwards;
    z-index: 1;
  }
  ::view-transition-new(root) {
    animation: vt-clip-in .46s cubic-bezier(.22,.61,.36,1) forwards;
    z-index: 2;
  }
  /* Paint the root bg underneath the transition so there is no white leak. */
  ::view-transition-group(root),
  ::view-transition-image-pair(root) {
    background: var(--bg, #09080a);
  }

  @keyframes vt-fade-out {
    from { opacity: 1; }
    to   { opacity: 0; }
  }
  @keyframes vt-fade-in {
    from { opacity: 0; transform: translateY(6px); }
    to   { opacity: 1; transform: translateY(0); }
  }
  @keyframes vt-clip-in {
    from { clip-path: circle(0% at var(--vt-x, 50%) var(--vt-y, 50%)); }
    to   { clip-path: circle(150% at var(--vt-x, 50%) var(--vt-y, 50%)); }
  }
  /* Keep bottom tab bar stable during transition */
  ::view-transition-old(btm-nav),
  ::view-transition-new(btm-nav) {
    animation: none;
  }

  /* Named transitions for persistent elements */
  .nav-logo { view-transition-name: site-logo; }
  ::view-transition-old(site-logo),
  ::view-transition-new(site-logo) {
    animation-duration: 0.25s;
  }
  /* Curtain removed — no longer needed; kept selectors as no-op for legacy JS */

  /* ═══════ FILM GRAIN ═══════ */
  .film-grain {
    position: fixed;
    inset: 0;
    z-index: 2;
    pointer-events: none;
    opacity: .035;
    background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 512 512' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='g'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='.75' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23g)'/%3E%3C/svg%3E");
    background-size: 256px;
    animation: grain 0.5s steps(4) infinite;
  }
  @keyframes grain {
    0%   { transform: translate(0, 0) }
    25%  { transform: translate(-5%, -5%) }
    50%  { transform: translate(5%, 5%) }
    75%  { transform: translate(-2%, 7%) }
    100% { transform: translate(7%, -2%) }
  }

  /* Light theme: film grain less visible, scroll progress softer */
  [data-theme="light"] .film-grain { opacity: .015; }
  [data-theme="light"] .scroll-progress {
    box-shadow: 0 0 4px var(--ac-line);
  }
  /* Light: scrollbar */
  [data-theme="light"] ::-webkit-scrollbar-thumb {
    background: linear-gradient(180deg, var(--ac), var(--ac2));
  }

  /* ═══════ SCROLL PROGRESS (CSS scroll-driven — no JS) ═══════ */
  .scroll-progress {
    position: fixed;
    top: 0; left: 0;
    height: 2px;
    width: 100%;
    background: linear-gradient(90deg, var(--ac), var(--ac3), var(--ac));
    z-index: 50;
    transform-origin: left;
    transform: scaleX(0);
    opacity: 1; /* override inline FOUC hide */
    box-shadow: 0 0 8px var(--ac-b3);
  }
  /* Progressive enhancement: CSS scroll-driven if supported */
  @supports (animation-timeline: scroll()) {
    .scroll-progress {
      animation: scroll-scale linear;
      animation-timeline: scroll();
    }
  }

  /* ═══════ LENIS SMOOTH SCROLL ═══════ */
  html.lenis, html.lenis body {
    height: auto;
  }
  .lenis.lenis-smooth {
    scroll-behavior: auto !important;
  }
  .lenis.lenis-smooth [data-lenis-prevent] {
    overscroll-behavior: contain;
  }

  /* ═══════ PILL — keyboard focus state (calculator, filters) ═══════ */
  .pill:focus-visible {
    outline: 2px solid var(--ac);
    outline-offset: 3px;
    border-radius: inherit;
  }

  /* ═══════ SUBMIT BUTTONS — disabled + busy states ═══════ */
  .submit-btn[disabled],
  .submit-btn[aria-busy="true"],
  button[type="submit"][disabled],
  button[type="submit"][aria-busy="true"],
  .btn[disabled],
  .btn-gold[disabled] {
    opacity: .55;
    cursor: wait;
    pointer-events: none;
    filter: saturate(.7);
  }
  [data-theme="light"] .submit-btn[disabled],
  [data-theme="light"] .submit-btn[aria-busy="true"] {
    opacity: .6;
    filter: saturate(.65) brightness(.95);
  }

  /* ═══════ PREMIUM BUTTON HOVER — subtle lift, soft easing, not jerky ═══════ */
  .btn, .btn-pill, .btn-primary, .btn-gold, .hero-cta, .submit-btn, .nav-cta, .float-cta-btn {
    transition: transform .22s cubic-bezier(.22,.61,.36,1),
                box-shadow .32s var(--ease-ios, cubic-bezier(.25,0,.2,1)),
                border-color .3s var(--ease-ios, cubic-bezier(.25,0,.2,1)),
                background-color .3s var(--ease-ios, cubic-bezier(.25,0,.2,1)),
                color .3s var(--ease-ios, cubic-bezier(.25,0,.2,1));
  }
  .btn:active, .btn-pill:active, .btn-gold:active, .hero-cta:active, .submit-btn:active {
    transform: translateY(0) scale(.985);
  }
  .nav-cta, .float-cta-btn, .btn, .btn-gold { will-change: transform; }

  /* Mobile: hide floating CTA (bottom tab bar handles primary actions) */
  @media (max-width: 768px) {
    .float-cta { display: none !important; }
  }

  /* ═══════ MOBILE PERFORMANCE ═══════ */
  @media (max-width: 768px) {
    /* 1B: Kill heavy orbs — keep only one, reduced */
    .orb-1 {
      width: 400px; height: 400px;
      filter: blur(60px);
    }
    .orb-2, .orb-3 { display: none; }

    /* 1B: Kill film grain (invisible at 3.5% on small screens) */
    .film-grain { display: none; }

    /* 1B: Kill aurora hue-rotate animation */
    .bg-mesh::after { animation: none; }

    /* 1C: Replace backdrop-filter with solid bg (very expensive on iOS) */
    .nav.scrolled {
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      background: rgba(9,8,12,.97);
    }
    [data-theme="light"] .nav.scrolled {
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      background: rgba(245,241,234,.97);
    }

    /* Fast view transitions on mobile — no clip-path (GPU-heavy) */
    ::view-transition-old(root) {
      animation: vt-fade-out .15s ease-in forwards;
    }
    ::view-transition-new(root) {
      animation: vt-fade-in .2s ease-out forwards;
    }
    ::view-transition-group(root) {
      animation-duration: .2s;
    }

    /* Disable reveal safety animation on mobile — causes flash */
    .reveal {
      animation: none !important;
    }
    .reveal.visible {
      animation: none !important;
    }

    /* Bottom tab bar persists during view transitions */
    .btm-nav {
      view-transition-name: btm-nav;
    }

    /* Bottom nav spacing */
    .main {
      padding-bottom: calc(64px + env(safe-area-inset-bottom, 0px));
    }
  }

  .skip-link {
    position: fixed;
    top: -100px;
    left: 16px;
    z-index: 99999;
    padding: 12px 24px;
    background: var(--ac);
    color: var(--bg);
    font: 500 13px/1 var(--fi);
    text-decoration: none;
    letter-spacing: .05em;
    transition: top .2s cubic-bezier(.4,0,.2,1);
  }
  .skip-link:focus {
    top: 16px;
    outline: 2px solid var(--ac);
    outline-offset: 2px;
  }

  /* ═══════ COOKIE CONSENT BANNER (152-ФЗ) ═══════ */
  .cookie-banner {
    position: fixed;
    left: 16px;
    right: 16px;
    bottom: 16px;
    z-index: 9000;
    pointer-events: none;
    opacity: 0;
    transform: translateY(20px);
    transition: opacity .28s var(--ease-ios, cubic-bezier(.25,0,.2,1)),
                transform .28s var(--ease-ios, cubic-bezier(.25,0,.2,1));
  }
  .cookie-banner[hidden] { display: none; }
  .cookie-banner.is-open {
    pointer-events: auto;
    opacity: 1;
    transform: translateY(0);
  }
  .cb-inner {
    max-width: 760px;
    margin: 0 auto;
    display: grid;
    grid-template-columns: 1fr auto;
    gap: 20px;
    align-items: center;
    padding: 18px 22px;
    background: rgba(15, 14, 16, .92);
    border: 1px solid rgba(212, 175, 55, .22);
    border-radius: 14px;
    backdrop-filter: blur(18px) saturate(140%);
    -webkit-backdrop-filter: blur(18px) saturate(140%);
    box-shadow: 0 20px 48px rgba(0, 0, 0, .42), 0 0 0 1px rgba(255,255,255,.02) inset;
  }
  [data-theme="light"] .cb-inner {
    background: rgba(250, 248, 242, .94);
    border-color: rgba(160, 130, 30, .25);
    box-shadow: 0 20px 48px rgba(20, 18, 12, .12), 0 0 0 1px rgba(0,0,0,.02) inset;
  }
  .cb-text { min-width: 0; }
  .cb-title {
    font: 500 14px/1.2 var(--fi, Inter, sans-serif);
    color: var(--t1);
    letter-spacing: .01em;
    margin: 0 0 4px;
  }
  .cb-desc {
    font: 400 12.5px/1.55 var(--fi, Inter, sans-serif);
    color: var(--t2);
    margin: 0;
  }
  .cb-desc a {
    color: var(--ac);
    text-decoration: underline;
    text-underline-offset: 2px;
    transition: color .2s var(--ease-ios, ease);
  }
  .cb-desc a:hover { color: var(--ac2); }
  .cb-actions { display: flex; gap: 8px; flex-shrink: 0; }
  .cb-btn {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    padding: 10px 18px;
    border-radius: 8px;
    font: 500 12.5px/1 var(--fi, Inter, sans-serif);
    letter-spacing: .04em;
    cursor: pointer;
    border: 1px solid var(--brd);
    background: transparent;
    color: var(--t2);
    transition: background-color .22s var(--ease-ios, ease),
                color .22s var(--ease-ios, ease),
                border-color .22s var(--ease-ios, ease),
                transform .18s var(--ease-ios, ease);
  }
  .cb-btn:hover {
    border-color: rgba(212,175,55,.45);
    color: var(--t1);
  }
  .cb-btn--gold {
    background: var(--ac);
    border-color: var(--ac);
    color: #1a1610;
  }
  .cb-btn--gold:hover {
    background: var(--ac2);
    border-color: var(--ac2);
    color: #1a1610;
    transform: translateY(-1px);
  }
  .cb-btn:focus-visible {
    outline: 2px solid var(--ac);
    outline-offset: 3px;
  }
  @media (max-width: 640px) {
    .cookie-banner {
      left: 12px; right: 12px;
      bottom: calc(72px + env(safe-area-inset-bottom, 0px));
    }
    .cb-inner {
      grid-template-columns: 1fr;
      gap: 14px;
      padding: 16px 18px;
      border-radius: 12px;
    }
    .cb-actions { width: 100%; }
    .cb-btn { flex: 1; padding: 12px 14px; font-size: 13px; }
    .cb-title { font-size: 13.5px; }
    .cb-desc { font-size: 12px; }
  }
</style>
